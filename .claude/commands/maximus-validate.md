---
description: Validate Maximus Loop project configuration with CLI checks and project-aware advisories
argument-hint: "[--json]"
---

Read ${CLAUDE_PLUGIN_ROOT}/skills/maximus-validate/SKILL.md and follow every step exactly as written. Start with Phase 1 immediately.

Do NOT skip steps or modify files unless the user explicitly asks to fix issues.

**Arguments received:** $ARGUMENTS
