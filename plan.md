[
  {
    "id": 1,
    "phase": 1,
    "category": "foundation",
    "feature": "Design token system",
    "description": "Create complete CSS design token system in ui/src/styles/globals.css based on the MojoVoice Style Guide. Replace old color variables with new Electric Night palette (deep navy + electric blue + acid green). Add typography tokens (JetBrains Mono + Inter), spacing scale, animation durations/easings, shadow tokens, and border tokens. Remove old emerald/teal color system.",
    "acceptance_criteria": [
      "CSS variables defined for all colors in style guide (--bg-void, --accent-primary, etc.)",
      "Typography tokens include font families, type scale, and usage classes",
      "Spacing tokens use 4px base unit (--space-1 through --space-16)",
      "Animation tokens include durations (--duration-fast, etc.) and easing curves",
      "Shadow tokens for brutal shadows (--shadow-brutal, --shadow-brutal-hover, --shadow-brutal-active)",
      "Border tokens for neubrutalist style (--border-brutal: #000000)",
      "Old emerald green (#10b981 in old system) replaced with electric blue (#3B82F6)",
      "File compiles without CSS errors",
      "npm run dev starts successfully"
    ],
    "passes": true
  },
  {
    "id": 2,
    "phase": 1,
    "category": "components",
    "feature": "Neubrutalist button component",
    "description": "Create reusable neubrutalist button component with primary, secondary, and ghost variants. Implement brutal shadow animation (shadow shifts on hover/press). Use electric blue (#3B82F6) for primary, outlined for secondary, minimal for ghost. Add loading state with pulse animation. Use Inter font for labels. Must match style guide specifications exactly.",
    "acceptance_criteria": [
      "Button component file created in ui/src/components/ui/",
      "Primary variant: solid blue bg, thick black border, 4px brutal shadow",
      "Hover state: shadow increases to 6px, button translates -2px",
      "Active state: shadow decreases to 2px, button translates +2px",
      "Secondary variant: transparent bg, blue border, glows on hover",
      "Ghost variant: transparent, minimal styling, hover bg-slate-800",
      "Loading state: pulse animation + disabled interactions",
      "Disabled state: 50% opacity, no shadow, no pointer",
      "All transitions use 150ms duration",
      "Uppercase text with tracking-wide",
      "Component used in at least one place (test page or existing UI)",
      "Visual matches style guide mockup"
    ],
    "passes": true
  },
  {
    "id": 3,
    "phase": 1,
    "category": "components",
    "feature": "Terminal section header",
    "description": "Create reusable section header component with terminal-inspired styling. Includes animated glyph (▸), uppercase title in JetBrains Mono, horizontal rule with gradient fade. Glyph pulses slowly (2s cycle). Header text is 12px uppercase with 0.12em letter-spacing in electric blue. Gradient rule fades from blue to transparent.",
    "acceptance_criteria": [
      "SectionHeader component created in ui/src/components/ui/",
      "Uses JetBrains Mono font",
      "Font size 12px (0.75rem)",
      "Uppercase with letter-spacing 0.12em",
      "Electric blue color (#3B82F6)",
      "Animated glyph (▸) with pulse animation (2s cycle, opacity 1 to 0.4)",
      "Horizontal rule with gradient (from-blue-500 to-transparent)",
      "Component accepts title prop",
      "Respects prefers-reduced-motion (disables glyph animation)",
      "Used in at least one section"
    ],
    "passes": false
  },
  {
    "id": 4,
    "phase": 1,
    "category": "components",
    "feature": "Neubrutalist card component",
    "description": "Create reusable neubrutalist card component with thick borders, sharp corners (no border-radius), hover state with blue border glow. Optional status badge in top-right corner. Dark background with elevated surface color. Supports hover effects (border changes to blue with glow shadow).",
    "acceptance_criteria": [
      "Card component created in ui/src/components/ui/",
      "2px solid border with --border-default (#334155)",
      "No border-radius (sharp corners)",
      "Dark background (--bg-surface: #151B2E)",
      "Hover state: border becomes blue (#3B82F6) with 0 0 20px glow",
      "Optional status badge prop (renders in top-right absolute position)",
      "Status badge styles: bg-green-500/20 text-green-400 border",
      "Transition duration 200ms",
      "Padding 24px (--space-6)",
      "Component accepts children and optional badge",
      "Used in at least one place"
    ],
    "passes": false
  },
  {
    "id": 5,
    "phase": 2,
    "category": "layout",
    "feature": "Single-page layout structure",
    "description": "Completely replace existing tab-based navigation (Dashboard/History/Settings) with single-page Mission Control layout. Remove all navigation tabs. Create main container with max-width 800px, centered, padding 48px. Structure: header (app name + settings icon), main content area, footer. This is a DESTRUCTIVE change - old navigation is removed entirely.",
    "acceptance_criteria": [
      "Old Navigation component removed or commented out",
      "Old tab-based views (Dashboard, History tabs) removed from routing",
      "New main layout file created (e.g., MissionControl.tsx)",
      "Header contains: 'MOJOVOICE' text (left) + settings gear icon (right)",
      "Main content container: max-width 800px, margin auto, padding 48px 24px",
      "Deep navy background (#0A0E1A) applied to entire app",
      "Settings icon uses Lucide React icon (Settings or Cog)",
      "Layout renders without errors",
      "App still compiles and runs (npm run dev)",
      "Old Dashboard/History content temporarily replaced with placeholder"
    ],
    "passes": false
  },
  {
    "id": 6,
    "phase": 2,
    "category": "ui",
    "feature": "Giant test recording button",
    "description": "Create giant neubrutalist 'TEST MIC' button as centerpiece of main screen. 280px wide, 120px tall, centered. Uses neubrutalist button styling with brutal shadow. Clicking triggers 5-second test recording and shows transcription result below. Electric blue with black border and brutal shadow animation.",
    "acceptance_criteria": [
      "Giant button rendered in center of main screen",
      "Dimensions: 280px wide × 120px tall",
      "Text: '⏺ TEST MIC' with microphone icon",
      "Uses neubrutalist button styling from component",
      "Brutal shadow animation on hover/press (4px → 6px → 2px)",
      "Click handler calls Tauri backend test_recording command",
      "Shows loading state during recording (5 seconds)",
      "Displays transcription result below button after completion",
      "Error handling if recording fails",
      "Margin bottom 48px to separate from status bar",
      "Visual matches style guide mockup"
    ],
    "passes": false
  },
  {
    "id": 7,
    "phase": 2,
    "category": "ui",
    "feature": "Status bar component",
    "description": "Build always-visible status bar showing: ready indicator (green dot + 'READY' text), current model with dropdown, language, and active microphone. Uses neubrutalist card styling with electric blue border. Model dropdown allows quick switching. Status updates in real-time from daemon.",
    "acceptance_criteria": [
      "StatusBar component created",
      "Shows green dot (●) + 'READY' when daemon running",
      "Shows red dot + 'OFFLINE' when daemon not running",
      "Displays current model name with dropdown icon",
      "Model dropdown functional (click opens, select switches model)",
      "Displays current language",
      "Displays active microphone name",
      "Uses neubrutalist card styling (2px border, padding 16px 24px)",
      "Status updates from daemon status query",
      "Positioned below test button with 48px margin",
      "Layout: dot + text on left, model dropdown center, language/mic right",
      "Font: JetBrains Mono for values, Inter for labels"
    ],
    "passes": false
  },
  {
    "id": 8,
    "phase": 2,
    "category": "ui",
    "feature": "Transcription card component",
    "description": "Create individual transcription card component for displaying single history entry. Shows: emoji icon, title (first few words), timestamp, word count, preview text (truncated), and hover actions (Copy, Delete). Uses neubrutalist card styling. Click to expand full text.",
    "acceptance_criteria": [
      "TranscriptionCard component created",
      "Uses Card component with neubrutalist styling",
      "Shows 📝 emoji icon",
      "Title: first 5-7 words of transcription",
      "Timestamp: relative time (2m ago, 5m ago, 1h ago)",
      "Word count: displayed as '• X words'",
      "Preview text: truncated to 2 lines with ellipsis",
      "Hover reveals action buttons: [Copy] [Delete]",
      "Copy button copies full text to clipboard",
      "Delete button shows confirmation before deleting",
      "Click card to toggle full text expansion",
      "Font: JetBrains Mono for timestamp/count, Inter for text",
      "Component accepts transcription data prop",
      "Actions functional (copy works, delete updates state)"
    ],
    "passes": false
  },
  {
    "id": 9,
    "phase": 2,
    "category": "ui",
    "feature": "Recent transcriptions section",
    "description": "Build 'RECENT TRANSCRIPTIONS' section showing last 3-5 transcription cards. Uses terminal section header component. Displays cards in vertical stack with 16px spacing. Includes 'View All →' link at bottom. Loads real transcription data from history.",
    "acceptance_criteria": [
      "Section uses SectionHeader component with title 'RECENT TRANSCRIPTIONS'",
      "Fetches transcription history from backend",
      "Displays last 5 transcriptions (or fewer if less available)",
      "Uses TranscriptionCard component for each entry",
      "Cards stacked vertically with 16px margin-bottom",
      "'View All →' button at bottom (placeholder for now)",
      "Shows empty state if no transcriptions: 'No transcriptions yet'",
      "Section positioned below status bar with 48px margin",
      "Real data loads from Tauri backend (get_history command)",
      "Updates when new transcription is added",
      "Copy/delete actions work on individual cards"
    ],
    "passes": false
  },
  {
    "id": 10,
    "phase": 2,
    "category": "ui",
    "feature": "Collapsible system status section",
    "description": "Build collapsible 'SYSTEM STATUS' section showing GPU info, memory usage, daemon status, and uptime. Uses terminal section header. Starts expanded, click header to collapse/expand. Shows real system data from diagnostics. Positioned at bottom of main screen.",
    "acceptance_criteria": [
      "Section uses SectionHeader component with title 'SYSTEM STATUS'",
      "Click header to toggle collapsed/expanded state",
      "Expanded shows: GPU name, memory (used/total), daemon status, uptime",
      "Collapsed shows just the header",
      "Smooth collapse animation (200ms ease)",
      "Uses JetBrains Mono for all values",
      "GPU: shows detected GPU or 'CPU Only'",
      "Memory: shows formatted GB (e.g., '2.3 GB / 24 GB')",
      "Daemon: shows ✓ Running or ✗ Stopped",
      "Uptime: shows formatted duration (e.g., '3h 42m')",
      "Data fetched from Tauri diagnostics command",
      "Positioned below recent transcriptions with 48px margin",
      "Collapse state persists in local storage"
    ],
    "passes": false
  },
  {
    "id": 11,
    "phase": 3,
    "category": "components",
    "feature": "Slide-in settings drawer",
    "description": "Create slide-in drawer component that appears from right side. Triggered by settings icon in header. Includes backdrop blur overlay, close button, smooth animation (300ms). Drawer is 400px wide, full height. Uses neubrutalist styling with deep navy background.",
    "acceptance_criteria": [
      "Drawer component created in ui/src/components/ui/",
      "Slides in from right side when open (translateX: 100% to 0)",
      "Width: 400px, height: 100vh",
      "Position: fixed, right: 0, top: 0",
      "Backdrop: rgba(10, 14, 26, 0.95) with blur(8px)",
      "Animation: 300ms cubic-bezier(0.16, 1, 0.3, 1)",
      "Close button (X icon) in top-right of drawer",
      "Escape key closes drawer",
      "Click backdrop closes drawer",
      "Background: --bg-surface (#151B2E)",
      "Border-left: 2px solid --border-default",
      "Z-index above main content",
      "Drawer accepts children (settings content)",
      "Open/close state managed by parent component"
    ],
    "passes": false
  },
  {
    "id": 12,
    "phase": 3,
    "category": "ui",
    "feature": "Settings drawer - Voice Recognition section",
    "description": "Build Voice Recognition section inside settings drawer. Includes terminal section header, model dropdown (shows downloaded models), and language dropdown. Uses existing Settings.tsx logic for model switching. Matches neubrutalist design.",
    "acceptance_criteria": [
      "Section header: 'VOICE RECOGNITION' using SectionHeader component",
      "Model dropdown: shows list of downloaded models",
      "Model dropdown: uses neubrutalist select styling (electric blue focus)",
      "Language dropdown: shows language options (English, Auto-detect, Spanish, etc.)",
      "Dropdowns use JetBrains Mono font for values",
      "Model switching works (calls Tauri switch_model command)",
      "Language switching updates config",
      "Section positioned at top of drawer content",
      "Labels use Inter font, values use JetBrains Mono",
      "Spacing: 24px padding, 16px between fields",
      "Integrates with existing Settings.tsx model switching logic"
    ],
    "passes": false
  },
  {
    "id": 13,
    "phase": 3,
    "category": "ui",
    "feature": "Settings drawer - Recording section",
    "description": "Build Recording section with max duration slider (dual input pattern) and audio device dropdown. Slider shows visual track + number input + preview text. Audio device dropdown lists available inputs. Updates config on change.",
    "acceptance_criteria": [
      "Section header: 'RECORDING' using SectionHeader component",
      "Max duration slider: range 10-300 seconds",
      "Slider visual: electric blue fill, custom 24px thumb with glow",
      "Number input alongside slider for precision",
      "Preview text below: 'Approximately X minutes' (calculated from seconds)",
      "Audio device dropdown: lists available devices from backend",
      "Both controls use neubrutalist styling",
      "Changes update config immediately (or on Save)",
      "Slider uses JetBrains Mono for value display",
      "Section positioned below Voice Recognition with 32px margin",
      "Values load from current config on drawer open"
    ],
    "passes": false
  },
  {
    "id": 14,
    "phase": 3,
    "category": "ui",
    "feature": "Settings drawer - Behavior section",
    "description": "Build Behavior section with toggle for 'Add trailing space' setting. Use liquid morph toggle animation. Simple section with just one toggle for now (future toggles can be added later).",
    "acceptance_criteria": [
      "Section header: 'BEHAVIOR' using SectionHeader component",
      "Toggle for 'Add trailing space' setting",
      "Toggle uses liquid morph animation (200ms elastic easing)",
      "Off state: gray background, muted thumb",
      "On state: blue background with glow, white thumb",
      "Thumb animates with elastic bounce effect",
      "Toggle updates config.output.append_space",
      "Label uses Inter font, positioned left of toggle",
      "Section positioned below Recording with 32px margin",
      "Current value loads from config on drawer open",
      "Change reflects immediately in UI"
    ],
    "passes": false
  },
  {
    "id": 15,
    "phase": 3,
    "category": "ui",
    "feature": "Settings drawer - Advanced section (collapsed)",
    "description": "Build collapsible Advanced section with power user settings: Model Path Override, Technical Vocabulary, Status Bar Integration, Save Audio Clips. Starts collapsed, click to expand. Uses neubrutalist inputs with terminal prompt styling.",
    "acceptance_criteria": [
      "Section header: 'ADVANCED' with click to expand/collapse",
      "Starts collapsed by default",
      "Expand animation: smooth 200ms ease",
      "Model Path Override: text input with terminal prompt (>)",
      "Technical Vocabulary: textarea for prompt biasing",
      "Status Bar Integration: text input for shell command",
      "Save Audio Clips: toggle + path input (path shows when enabled)",
      "All inputs use neubrutalist styling (electric blue focus glow)",
      "Inputs use JetBrains Mono font",
      "Section positioned below Behavior with 32px margin",
      "Collapse state persists in local storage",
      "Values load from config on drawer open"
    ],
    "passes": false
  },
  {
    "id": 16,
    "phase": 3,
    "category": "ui",
    "feature": "Settings drawer - Save/Reset actions",
    "description": "Add Save and Reset buttons at bottom of settings drawer. Save commits changes to config and restarts daemon if needed (shows banner). Reset reloads original values. Buttons use neubrutalist styling. Smart restart logic (only show banner if daemon settings changed).",
    "acceptance_criteria": [
      "Save button: primary neubrutalist style, fixed at drawer bottom",
      "Reset button: ghost style, positioned left of Save",
      "Save button text: 'Save Changes'",
      "Reset button text: 'Reset'",
      "Save calls Tauri save_config command",
      "Save shows success state: green border + 'Saved!' text for 3s",
      "Reset reloads config values and updates all fields",
      "Smart restart logic: only show restart banner if model/audio/language changed",
      "Buttons positioned with 24px padding from drawer bottom",
      "Buttons use gap-3 spacing between them",
      "Loading state during save (button shows spinner)"
    ],
    "passes": false
  },
  {
    "id": 17,
    "phase": 4,
    "category": "components",
    "feature": "Full history modal overlay",
    "description": "Create modal overlay component for full transcription history view. Triggered by 'View All →' button. Fullscreen overlay with backdrop blur, centered content box (900px wide), search input at top, scrollable card list. Close with X button or Escape key.",
    "acceptance_criteria": [
      "Modal component created in ui/src/components/ui/",
      "Fixed position covering entire viewport (inset: 0)",
      "Backdrop: rgba(10, 14, 26, 0.95) with blur(8px)",
      "Content box: 900px wide, max-height 80vh, centered",
      "Content background: --bg-surface with 2px border",
      "Header: 'TRANSCRIPTION HISTORY' + close button (X)",
      "Search input at top with neubrutalist styling",
      "Scrollable area for history cards (virtual scrolling if >50 items)",
      "Close button (X icon) in top-right",
      "Escape key closes modal",
      "Click backdrop closes modal",
      "Fade-in animation (200ms)",
      "Z-index above settings drawer",
      "Modal accepts children (history content)"
    ],
    "passes": false
  },
  {
    "id": 18,
    "phase": 4,
    "category": "ui",
    "feature": "Full history - Search and filters",
    "description": "Implement search functionality in history modal. Search input filters transcriptions by text content (real-time). Add filter button for date range and word count. Search uses JetBrains Mono font, electric blue focus glow.",
    "acceptance_criteria": [
      "Search input positioned at top of modal",
      "Placeholder text: 'Search transcriptions...'",
      "Real-time filtering as user types (debounced 300ms)",
      "Search matches transcription text content (case-insensitive)",
      "Filter button next to search (shows filter options)",
      "Filter options: date range (today, this week, this month, all time)",
      "Filter options: word count range (short <20, medium 20-50, long >50)",
      "Filters combine with search (AND logic)",
      "Results update immediately when filters change",
      "Shows count: 'Showing X of Y transcriptions'",
      "Clear search button (X) appears when text entered",
      "Search uses neubrutalist input styling"
    ],
    "passes": false
  },
  {
    "id": 19,
    "phase": 4,
    "category": "ui",
    "feature": "Full history - Export and bulk actions",
    "description": "Add export and bulk action buttons to history modal footer. Export All button downloads all transcriptions as JSON. Clear History button deletes all with confirmation. Buttons use neubrutalist styling.",
    "acceptance_criteria": [
      "Export All button at modal footer (left side)",
      "Clear History button at modal footer (right side)",
      "Export All: downloads JSON file with all transcriptions",
      "JSON includes: text, timestamp, word_count, model_used",
      "Filename: mojovoice-history-YYYY-MM-DD.json",
      "Clear History shows confirmation dialog before deleting",
      "Confirmation uses neubrutalist dialog styling",
      "Clear History calls Tauri clear_history command",
      "Both buttons disabled during operations (loading state)",
      "Success message after export: 'Exported X transcriptions'",
      "Success message after clear: 'History cleared'",
      "Buttons use ghost neubrutalist style"
    ],
    "passes": false
  },
  {
    "id": 20,
    "phase": 5,
    "category": "ux",
    "feature": "Keyboard shortcuts system",
    "description": "Implement global keyboard shortcuts: Space (test mic when focused), Cmd+, (open settings), Cmd+H (open history), Cmd+K (search history), Cmd+C (copy last transcription), Escape (close modals/drawers). Show shortcuts in tooltips.",
    "acceptance_criteria": [
      "Space: triggers test recording when main screen focused",
      "Cmd+, or Ctrl+,: opens settings drawer",
      "Cmd+H or Ctrl+H: opens history modal",
      "Cmd+K or Ctrl+K: opens history modal with search focused",
      "Cmd+C or Ctrl+C: copies last transcription to clipboard",
      "Escape: closes open modal or drawer (whichever is on top)",
      "Shortcuts work globally when app is focused",
      "Tooltips show shortcuts (e.g., settings icon tooltip: 'Settings (Cmd+,)')",
      "Visual feedback when shortcut triggered (brief highlight)",
      "Shortcuts disabled when input fields focused (except Escape)",
      "Works on both macOS (Cmd) and Linux/Windows (Ctrl)"
    ],
    "passes": false
  },
  {
    "id": 21,
    "phase": 5,
    "category": "animation",
    "feature": "Micro-interactions and polish",
    "description": "Add all signature micro-interactions from style guide: button brutal shadow shift, toggle liquid morph, focus electric glow, loading scanning animation. Add hover states to all interactive elements. Ensure all animations honor prefers-reduced-motion.",
    "acceptance_criteria": [
      "Button press: brutal shadow shifts (4px → 6px → 2px) in 150ms",
      "Toggle: liquid morph animation with elastic easing (200ms)",
      "Focus states: electric glow ring (2px blue outline + 20px glow shadow)",
      "Loading states use scanning animation (blue glow moves left-to-right)",
      "All hover states use 150-200ms transitions",
      "Card hover: border changes to blue with glow (200ms)",
      "Dropdown open: slide down + fade (250ms)",
      "Drawer slide: smooth ease-out (300ms)",
      "Modal fade: 200ms opacity transition",
      "All animations use GPU-accelerated properties (transform, opacity)",
      "prefers-reduced-motion: all animations disabled or simplified",
      "No animation exceeds 400ms duration"
    ],
    "passes": false
  },
  {
    "id": 22,
    "phase": 5,
    "category": "accessibility",
    "feature": "Accessibility enhancements",
    "description": "Ensure full WCAG 2.2 Level AA compliance. Add proper ARIA labels, semantic HTML, screen reader support, keyboard navigation, and high contrast. Test with keyboard-only navigation. Add skip links and focus management.",
    "acceptance_criteria": [
      "All interactive elements have visible focus states (electric blue ring)",
      "Proper semantic HTML (button, nav, main, section, article)",
      "ARIA labels on all icon buttons (e.g., settings icon: aria-label='Settings')",
      "ARIA live regions for dynamic content (transcription updates, status changes)",
      "ARIA expanded/collapsed states on collapsible sections",
      "Keyboard navigation works without mouse (Tab, Enter, Space, Escape, Arrow keys)",
      "Skip to main content link at top for screen readers",
      "Focus trap in modals (Tab cycles within modal when open)",
      "Color contrast meets WCAG AA (7:1 for text, 3:1 for UI components)",
      "Screen reader announces all content correctly (test with NVDA/Orca)",
      "All images have alt text (or aria-hidden if decorative)",
      "Form labels properly associated with inputs"
    ],
    "passes": false
  },
  {
    "id": 23,
    "phase": 5,
    "category": "performance",
    "feature": "Performance optimization",
    "description": "Optimize UI performance: code splitting, lazy loading, virtual scrolling for long lists, image optimization, font subsetting. Ensure 60fps animations. Meet performance budget (<100KB CSS, <200KB JS, <1s first paint).",
    "acceptance_criteria": [
      "Route-based code splitting (main, settings, history as separate chunks)",
      "Lazy load history modal (only loads when opened)",
      "Virtual scrolling for history list (only render visible items)",
      "Font files subset to only needed glyphs (JetBrains Mono, Inter)",
      "All animations run at 60fps (use GPU-accelerated properties)",
      "No layout shift during page load (reserve space for async content)",
      "Images use WebP format where supported",
      "Bundle size: CSS <100KB, JS <200KB (check with build)",
      "Lighthouse score: Performance >90",
      "Time to interactive: <2s on 3G",
      "First contentful paint: <1s",
      "No unnecessary re-renders (use React DevTools Profiler)"
    ],
    "passes": false
  },
  {
    "id": 24,
    "phase": 5,
    "category": "testing",
    "feature": "Manual testing and bug fixes",
    "description": "Thoroughly test all UI functionality manually. Test all interactions, keyboard shortcuts, responsive behavior, error states, edge cases. Fix any bugs discovered. Test on different screen sizes and with reduced motion enabled.",
    "acceptance_criteria": [
      "Test recording button works (records, shows transcription)",
      "Status bar updates correctly when daemon status changes",
      "Model switching works from status bar dropdown",
      "Recent transcriptions load and display correctly",
      "Copy/delete actions work on transcription cards",
      "System status section collapses/expands smoothly",
      "Settings drawer opens/closes smoothly",
      "All settings controls functional (dropdowns, sliders, toggles)",
      "Save/reset buttons work correctly",
      "History modal opens with correct data",
      "Search and filters work in history",
      "Export and clear history work",
      "All keyboard shortcuts functional",
      "No console errors during any operation",
      "Responsive on different window sizes (test 800px, 1024px, 1440px)",
      "Reduced motion preference respected (no animations)",
      "All edge cases handled (empty history, no models, daemon offline)"
    ],
    "passes": false
  }
]
